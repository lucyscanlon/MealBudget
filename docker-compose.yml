services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: wlt
      POSTGRES_PASSWORD: wlt_dev
      POSTGRES_DB: weight_loss_tracker
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
